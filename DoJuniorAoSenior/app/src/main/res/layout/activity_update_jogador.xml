<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".UpdateJogadorActivity">

    <TextView
        android:id="@+id/editJogadorHeading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginTop="16dp"
        android:text="Editar Jogador"
        android:textColor="@color/purple"
        android:textSize="24sp" />

    <ImageView
        android:id="@+id/updateSaveButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/baseline_done_24"
        android:layout_alignParentEnd="true"
        android:layout_alignTop="@id/editJogadorHeading"
        android:layout_marginEnd="6dp"/>

    <EditText
        android:id="@+id/updateNomeJogador"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:hint="Insira o nome do jogador"
        android:padding="12dp"
        android:textSize="20sp"
        android:layout_below="@id/editJogadorHeading"
        android:background="@drawable/purple_border"
        android:layout_marginTop="16dp"
        android:maxLines="1"/>

    <EditText
        android:id="@+id/updateDinheiroJogador"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_below="@id/updateNomeJogador"
        android:layout_marginTop="6dp"
        android:background="@drawable/purple_border"
        android:hint="Insira o dinheiro do jogador"
        android:maxLines="1"
        android:padding="12dp"
        android:textSize="20sp" />

</RelativeLayout>